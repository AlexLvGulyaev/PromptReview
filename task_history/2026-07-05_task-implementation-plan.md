# Задача: Разработка IMPLEMENTATION_PLAN.md

**Дата:** 2026-07-05
**Статус:** В работе

---

## Исходное задание

**Контекст:**
- Discovery завершён.
- PROJECT_GOAL.md является актуальным техническим заданием.
- PROJECT_STATE.md, SPEC.md и README.md актуализированы и утверждены.

**Следующий этап проекта — подготовка IMPLEMENTATION_PLAN.md.**

**Требования:**
1. На основании PROJECT_GOAL.md и SPEC.md разработать IMPLEMENTATION_PLAN.md.
2. План должен описывать реализацию Prompt Review Service поэтапно, без пропусков.
3. Для каждого этапа определить:
   - цель;
   - состав работ;
   - создаваемые артефакты;
   - зависимости;
   - критерий завершения.
4. План должен охватывать:
   - реализацию FastAPI API-слоя;
   - backend-абстракцию (LangFlow/LangChain);
   - реализацию Telegram UI;
   - реализацию Web UI;
   - тестирование;
   - подготовку GitHub-публикации.
5. IMPLEMENTATION_PLAN должен быть согласован с PROJECT_GOAL.md, PROJECT_STATE.md и SPEC.md и не противоречить им.
6. Код на данном этапе не писать.

---

## Выполнение

### 1. Проанализированы исходные документы

**Из PROJECT_GOAL.md извлечено:**
- 10 ключевых шагов (архитектурное решение → реализация → публикация)
- Структура api/ (main.py, requirements.txt, .env.example, README.md, tests.md)
- Endpoint POST /review с JSON-контрактом
- Два UI-сценария: Telegram Bot и Web UI
- Production-требования: HTTPS, API key, CORS, logging, rate limiting

**Из SPEC.md извлечено:**
- Полный API specification (GET /, GET /health, POST /review)
- Pydantic-модели для request/response
- Backend-варианты: LangFlow Adapter, LangChain Adapter
- UI-сценарии: Telegram Bot и Web UI
- Production Requirements: безопасность, надёжность, эксплуатация
- Состав артефактов PEl06
- Definition of Done

### 2. Определены этапы реализации

**Выделено 8 этапов:**

| Этап | Название | Цель |
|------|----------|------|
| 1 | Структура проекта | Создать базовую структуру `api/` с конфигурацией |
| 2 | Pydantic-модели | Определить модели для request/response |
| 3 | Backend-адаптеры | Реализовать абстракцию для LangFlow/LangChain |
| 4 | FastAPI-сервер | Реализовать три эндпоинта: `/`, `/health`, `/review` |
| 5 | Тестирование API | Создать тестовые сценарии, выполнить тесты |
| 6 | Telegram Bot UI | Реализовать Telegram Bot |
| 7 | Web UI | Реализовать Web-форму |
| 8 | Документация | Подготовить README, Docker, публикацию |

### 3. Для каждого этапа определены

- Цель
- Состав работ (задачи)
- Создаваемые артефакты (с примерами кода)
- Зависимости от предыдущих этапов
- Критерий завершения (checkboxes)

### 4. Добавлены дополнительные разделы

- **Сводная таблица этапов** — обзор зависимостей
- **Риски и митигация** — анализ потенциальных проблем
- **Definition of Done** — критерии завершения проекта
- **Следующие шаги после завершения** — post-PEl06 roadmap

### 5. Согласование с документами

**Проверено согласование с:**

- PROJECT_GOAL.md — все 10 шагов отражены в этапах
- PROJECT_STATE.md — следующие шаги соответствуют этапам 1-8
- SPEC.md — JSON-контракт, API specification, production requirements

---

## Созданные артефакты

| Файл | Строк | Назначение |
|------|-------|------------|
| `docs/IMPLEMENTATION_PLAN.md` | ~700 | Технический план реализации PEl06 |

---

## Результат

✅ **IMPLEMENTATION_PLAN.md** создан с:
- 8 поэтапными этапами
- Целями, работами, артефактами, зависимостями, критериями для каждого этапа
- Охвачены: FastAPI, backend-абстракция, Telegram UI, Web UI, тестирование, публикация
- Согласован с PROJECT_GOAL.md, PROJECT_STATE.md, SPEC.md
- Без противоречий с утверждёнными документами

---

## Корректировки после утверждения

### 1. Убрана оценка длительности

Удалена строка "Продолжительность: 8 этапов, ориентировочно 1-2 дня на этап."

IMPLEMENTATION_PLAN — технический план реализации, а не календарный план.

### 2. Скорректирован этап 5

**До:** Этап 5 содержал E2E-тесты Telegram и Web

**После:** Этап 5 "Тестирование FastAPI" — только тестирование FastAPI-эндпоинтов:
- `GET /`, `GET /health`, `POST /review`
- Позитивные/негативные тесты
- Граничные случаи
- Проверка JSON-контракта
- Swagger UI, curl

E2E-тесты Telegram перенесены в этап 6 (критерии завершения).
E2E-тесты Web перенесены в этап 7 (критерии завершения).

### 3. Изучена существующая инфраструктура `infra/`

**Обнаружено:**
- `docker-compose.langflow.yml` — конфигурация LangFlow + PostgreSQL
- `.env.example`, `.env`, `.gitignore` — уже существуют
- LangFlow развёрнут на VPS через существующий docker-compose
- Сеть `n8n_default` используется для публикации через Traefik

### 4. Скорректирован этап 8

**Принцип:** не создавать параллельную инфраструктуру.

**Изменения:**
- `.env.example` убран из артефактов этапа 1 (существует в `infra/`)
- Этап 8 создаёт `infra/docker-compose.api.yml` и `infra/Dockerfile.api`
- Этап 8 актуализирует `infra/.env.example` и `infra/README.md`
- Используется существующая сеть `n8n_default`

### 5. Обновлена сводная таблица

| Этап | Ключевые артефакты |
|------|-------------------|
| 1 | `api/`, `requirements.txt`, `README.md` (без `.env.example`) |
| 5 | `tests.md`, скриншоты API (без E2E Telegram/Web) |
| 6 | `telegram/`, E2E-тесты Telegram |
| 7 | `web/`, E2E-тесты Web |
| 8 | `docker-compose.api.yml`, `Dockerfile.api`, актуализация `infra/` |

---

## Финальная актуализация

### 1. Зафиксирован архитектурный принцип

**Принцип:** "Один сервисный контур — один docker-compose файл."

- LangFlow разворачивается через `infra/docker-compose.langflow.yml`
- Prompt Review FastAPI разворачивается через `infra/docker-compose.api.yml`
- Compose-файлы описывают разные сервисные контуры и не дублируют друг друга
- Взаимодействие через внешнюю сеть `n8n_default`

Добавлено в раздел 8.2 "Контекст инфраструктуры" IMPLEMENTATION_PLAN.

### 2. Проверена сводная таблица этапов

Исправлено:
- Этап 1: убран `.env.example` из артефактов (он в `infra/`)
- Добавлено примечание: "`.env.example` находится в `infra/` и актуализируется на этапе 8"
- Этап 8: ключевые артефакты — `infra/docker-compose.api.yml`, `infra/Dockerfile.api`

### 3. Выполнена внутренняя проверка IMPLEMENTATION_PLAN

**Проверка на противоречия между этапами:**
- ✅ Все зависимости этапов корректны (1→2→3→4→5, 4+5→6, 4+5→7, 5+6+7→8)
- ✅ Артефакты не дублируются между этапами
- ✅ `.env.example` фигурирует только в этапе 8 (в `infra/`)

**Согласованность с PROJECT_GOAL.md:**
- ✅ Архитектура Telegram/Web → FastAPI → Prompt Review Engine → LangFlow/LangChain
- ✅ Endpoint `/review` (не `/multiply`)
- ✅ JSON-контракт из PEl05 сохранён
- �) Эндпоинты: GET /, GET /health, POST /review
- ✅ Структура api/ соответствует (с учётом переноса `.env.example` в `infra/`)

**Согласованность с PROJECT_STATE.md:**
- ✅ Архитектурная эволюция PEl03→PEl04→PEl05→PEl06 отражена
- ✅ Backend-варианты LangFlow/LangChain отражены в этапе 3
- ✅ Следующий шаг "Реализация FastAPI endpoint /review" отражён

**Согласованность со SPEC.md:**
- ✅ API Specification отражён в этапах 2 (модели) и 4 (эндпоинты)
- ✅ JSON-контракт из SPEC.md соответствует Pydantic-моделям
- �) UI-сценарии Telegram и Web отражены в этапах 6 и 7
- ✅ Production requirements отражены в этапе 8

**Согласованность с существующей структурой проекта:**
- ✅ Этап 8 использует существующий каталог `infra/`
- �) Не создаёт параллельную инфраструктуру
- �) Формулировки не интерпретируются как дублирование

### 4. Проверка ссылок на `.env.example`

Все 7 вхождений `.env.example` в IMPLEMENTATION_PLAN.md:
- Строка 61: примечание в этапе 1 (корректно — ссылается на `infra/`)
- Строка 650: таблица существующих артефактов в `infra/`
- Строка 677: задача 8.1.6 — актуализировать `infra/.env.example`
- Строка 692: артефакты этапа 8 — `.env.example (актуализирован)`
- Строка 746: пример переменных для `.env.example`
- Строка 792: критерий завершения — `infra/.env.example` актуализирован
- Строка 813: примечание в сводной таблице

Все ссылки корректны — указывают на `infra/` и этап 8.

---

## Статус

**Утверждён** — IMPLEMENTATION_PLAN готов к реализации.