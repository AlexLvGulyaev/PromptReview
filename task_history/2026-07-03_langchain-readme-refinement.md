# Задача: Корректировка формулировок в README

**Дата:** 2026-07-03
**Статус:** Выполнено

---

## Исходное задание

Внести два точечных изменения в `langchain/README.md`:

1. Скорректировать формулировку принятия решения — заменить утверждение "AgentExecutor самостоятельно решает..." на технически корректную формулировку, где ясно что LLM в составе AgentExecutor принимает решение
2. Добавить сравнительную таблицу режимов работы после раздела "Состав реализации"

---

## Выполненные изменения

### 1. Исправлена формулировка о принятии решений

**Было:**
> AgentExecutor самостоятельно решает, нужно ли вызывать инструмент на основе анализа задачи.

**Стало:**
> В процессе работы LLM в составе AgentExecutor анализирует задачу и при необходимости инициирует вызов доступного инструмента.

**Причина:** Избежать впечатления, что решение принимает AgentExecutor независимо от LLM. Технически корректно указать, что именно LLM внутри AgentExecutor принимает решение о вызове инструмента.

### 2. Добавлена сравнительная таблица режимов

После раздела "Состав реализации" добавлена таблица:

| Режим | Запуск | Особенность |
|-------|--------|-------------|
| Chain | main.py | Прямая цепочка без использования инструментов |
| AgentExecutor | main_agent.py | Агентный режим с Tool Calling и использованием prompt_metrics |

**Причина:** Упростить пользователю выбор режима работы перед изучением архитектур.

---

## Изменённые файлы

- **Обновлён:** `langchain/README.md`